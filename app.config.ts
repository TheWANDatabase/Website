export default defineAppConfig({
  ui: {
    safelistcolors: [
      'scorch',
      'poppy'
    ],
    icons: ['mdi'],
    primary: 'zinc',
    gray: 'slate',
    notifications: {
      // Show toasts at the top right of the screen
      position: 'bottom-0 right-0'
    }
  }
})
